{
	"name": "@kivcode/plugin-analytics",
	"version": "0.0.0",
	"private": true,
	"description": "Minimal example plugin (click counter) demonstrating the Kiv plugin API. Not published — see docs/plugins.md for real plugin examples (@kivcode/plugin-seo, @kivcode/plugin-a11y).",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.mjs"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "unbuild",
		"test": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@kivcode/engine": "workspace:*"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"unbuild": "^3.6.1",
		"vitest": "^4.1.9"
	}
}
